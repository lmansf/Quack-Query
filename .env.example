# Anthropic API key used by api/query.ts (server-side only; never shipped to the browser).
# Copy this file to .env for local development. On Vercel, set it as a project environment variable.
ANTHROPIC_API_KEY=sk-ant-...
